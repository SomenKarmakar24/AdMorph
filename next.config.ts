import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'ik.imagekit.io',
      'imgv3.fotor.com',
      'ai-avatar-generator.com',
      'res.jogg.ai',
      'amajova.com'
    ]
  }
};

export default nextConfig;
